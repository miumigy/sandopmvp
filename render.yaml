services:
  - type: web
    name: sandopmvp
    env: node
    plan: free
    autoDeploy: true
    buildCommand: npm install && npm test && npm run build
    startCommand: npm start
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: sandopmvp-db
          property: connectionString

databases:
  - name: sandopmvp-db
    plan: free
    databaseName: sandopmvp
    user: sandopmvp
